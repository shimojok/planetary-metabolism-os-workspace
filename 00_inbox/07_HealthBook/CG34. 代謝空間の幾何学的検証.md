貴方の判断に任せます。

---

では、**可視化を先行**します。  
理由は明確です：

1. 294×15空間が“構造”を持っているか確認できる
    
2. ルールベース骨格（A）が機能しているか検証できる
    
3. 次段の疾患テンソル接続の安定性を担保できる
    

---

# フェーズ1：代謝空間の幾何学的検証

## 目的

- クラスタ分離性の確認
    
- 経路主成分の抽出
    
- 経路間相関の把握
    

---

# Ⅰ. PCAによる構造確認（一次検証）

```python
from sklearn.decomposition import PCA
import matplotlib.pyplot as plt

def visualize_pca(matrix, labels):

    pca = PCA(n_components=2)
    reduced = pca.fit_transform(matrix)

    plt.figure()
    plt.scatter(reduced[:,0], reduced[:,1])
    plt.title("MBT15 PCA Projection")
    plt.xlabel("PC1")
    plt.ylabel("PC2")
    plt.show()

    print("Explained variance:", pca.explained_variance_ratio_)
```

### 見るべきポイント

- PC1がどの経路に強く依存するか
    
- PC2が炎症系か代謝系か
    
- クラスタが自然分離しているか
    

---

# Ⅱ. UMAPによる非線形構造検出（本命）

```python
import umap

def visualize_umap(matrix, labels):

    reducer = umap.UMAP(n_neighbors=15, min_dist=0.1, random_state=42)
    embedding = reducer.fit_transform(matrix)

    plt.figure()
    plt.scatter(embedding[:,0], embedding[:,1])
    plt.title("MBT15 UMAP Projection")
    plt.show()
```

### 期待される結果

- 消化系群
    
- 免疫系群
    
- 神経系群
    
- 代謝系群
    

が自然に分かれるはずです。

---

# Ⅲ. 経路寄与ヒートマップ

```python
import seaborn as sns

def heatmap(matrix):

    plt.figure()
    sns.heatmap(matrix, cmap="viridis")
    plt.title("294 × 15 Pathway Heatmap")
    plt.show()
```

これで

- どの経路が支配的か
    
- スパース性は適切か
    
- 偏りがないか
    

が分かります。

---

# 成功判定基準

✔ 主成分上位2軸で40%以上説明  
✔ UMAPで明確な群構造  
✔ 経路分布が極端に偏らない

---

# 可視化後の戦略

構造が健全なら：

→ 疾患137種テンソル接続へ進む

構造が崩れているなら：

→ 重みαβγを再調整

---

# 次の一手

この可視化を実行するにあたり確認です：

現在のJSONは  
「294完全版JSONマスター」で進めますか？

それとも  
「106サンプル構造」を統合した最新版で進めますか？

正確性を取るなら完全版JSONで行くべきです。

[[CG35. 294×15空間の統計構造検証]]
