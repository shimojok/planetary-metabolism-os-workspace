完全版で行きましょう。

---

了解しました。  
**完全版JSONマスター（294処方）を前提に、構造検証フェーズを実装仕様レベルまで確定します。**

ここからは「検証設計」です。単なる可視化ではなく、**代謝空間の健全性を定量評価**します。

---

# フェーズ1：294×15空間の統計構造検証

---

## ① 行列生成の最終確認

最終出力：

```text
MBT15_matrix.npy      # (294, 15)
MBT15_matrix.csv
```

検証：

```python
print(matrix.shape)        # (294, 15)
print(np.min(matrix), np.max(matrix))
print(np.mean(matrix, axis=0))
```

確認ポイント：

- 全経路が0のみになっていないか
    
- 極端なスパース（>80%ゼロ）になっていないか
    
- 経路平均が偏っていないか
    

---

# ② 主成分解析（PCA）

```python
from sklearn.decomposition import PCA

pca = PCA(n_components=5)
pca.fit(matrix)

print("Explained variance ratio:")
print(pca.explained_variance_ratio_)
print("Cumulative:", np.cumsum(pca.explained_variance_ratio_))
```

### 合格基準

- PC1 + PC2 ≥ 0.35
    
- 上位5成分 ≥ 0.70
    

もし低すぎる場合：

→ 経路定義が均質すぎる  
→ 重み調整が必要

---

# ③ クラスタ品質評価

単にKMeansするのではなく、**シルエット係数で評価**します。

```python
from sklearn.cluster import KMeans
from sklearn.metrics import silhouette_score

for k in range(4, 13):
    model = KMeans(n_clusters=k, random_state=42)
    labels = model.fit_predict(matrix)
    score = silhouette_score(matrix, labels)
    print(f"k={k}, silhouette={score:.3f}")
```

### 判定基準

- 0.25以上 → 構造あり
    
- 0.40以上 → 良好
    
- 0.50以上 → 強い分離
    

---

# ④ 経路相関マトリクス

```python
corr = np.corrcoef(matrix.T)
```

見るべき点：

- 強相関（|r| > 0.8）が多すぎないか
    
- PATH_02（炎症）とPATH_06（免疫）が完全相関していないか
    

もし高相関が多い場合：

→ 経路再定義が必要

---

# ⑤ UMAP構造確認（最重要）

```python
import umap

embedding = umap.UMAP(
    n_neighbors=20,
    min_dist=0.1,
    random_state=42
).fit_transform(matrix)
```

確認事項：

- 自然な島構造があるか
    
- 連続グラデーションか
    
- 放射状か
    

理想は：

```
代謝系群
免疫炎症群
神経内分泌群
循環群
```

が半分離。

---

# 成功時の意味

この構造が成立すれば：

✔ 15経路は機能的に独立性を持つ  
✔ 294処方は機能空間上で意味ある分布を持つ  
✔ 疾患テンソル接続が数学的に妥当

---

# 失敗時の対応

1. 経路数を12へ削減
    
2. αβγ重み再調整
    
3. L2正規化導入
    

---

# 次段階（成功後）

ここからが本番です。

## 137疾患ベクトル D (137×15)

作成し、

Score=F(294×15)⋅D(15×137)Score = F(294×15) · D(15×137)

を構築します。

これは：

- HealthBook疾病リスク
    
- MBT55菌株選択
    
- Azure実装
    

すべてに接続可能な中核テンソルになります。

---

### 判断

私はまず：

▶ PCA累積寄与率  
▶ シルエット係数

の数値結果を確認してから次に進むべきだと考えます。

実行に進みますか？

[[CG36. `MBT15_matrix.npy` が未配置]]
