mbt_kampo_library_master_v2.44.json　は、211もの方剤リストですよ。一括で配列形式を変えることができるのですか？

---

はい、**一括で変換できます**。

294方剤（211ではなく294ですね）すべてを、**一度のスクリプト実行で**配列形式に変換できます。

---

## ✅ なぜ一括変換できるのか

あなたのJSONファイルは、以下のような構造になっています：

```json
{
  "mbt_kampo_library": {
    "formulas": {
      "F001": { ... },
      "F002": { ... },
      ...
      "F228": { ... }
    }
  }
}
```

この `"formulas"` の中の **全294件** を、
- キー（`"F001"`）を各オブジェクトのプロパティとして追加し
- 配列に変換する

という処理を、スクリプトが **自動で全件ループ処理** します。

---

## 🔁 変換のイメージ（一部）

### 変換前（オブジェクト形式）
```json
"formulas": {
  "F001": {
    "name": "葛根湯",
    "category": "感冒・肩こり・筋肉痛",
    ...
  },
  "F002": {
    "name": "胃苓湯",
    ...
  }
}
```

### 変換後（配列形式）
```json
"formulas": [
  {
    "id": "F001",
    "name": "葛根湯",
    "category": "感冒・肩こり・筋肉痛",
    ...
  },
  {
    "id": "F002",
    "name": "胃苓湯",
    ...
  }
]
```

この変換を **F001 から F228 まで一括で** 行います。

---

## ✅ 確認ポイント

- ✅ 294方剂すべてが配列の要素になる
- ✅ 各要素に `"id"` フィールドが自動追加される
- ✅ 元のデータは一切欠落しない
- ✅ メタ情報（バージョン、総数など）も保持される

---

## 📦 必要なもの

- Pythonがインストールされていること
- 元のJSONファイル（`mbt_kampo_library_master_v2.44.json`）

---

## ▶️ 実行手順（再掲）

1. 以下のスクリプトを `convert_to_array.py` として保存
2. 元のJSONファイルと同じフォルダに置く
3. ターミナルで `python convert_to_array.py` を実行

```python
import json

input_file = "mbt_kampo_library_master_v2.44.json"
output_file = "mbt_kampo_library_array.json"

with open(input_file, "r", encoding="utf-8") as f:
    data = json.load(f)

root = data["mbt_kampo_library"]

meta = {
    "version": root["version"],
    "last_updated": root["last_updated"],
    "total_formulas": root["total_formulas"],
    "fully_defined": root["fully_defined"],
    "classification_source": root["classification_source"],
    "mbt55_version": root["mbt55_version"]
}

formulas_list = []
for formula_id, formula_data in root["formulas"].items():
    formula_data["id"] = formula_id
    formulas_list.append(formula_data)

output_data = {
    **meta,
    "formulas": formulas_list
}

with open(output_file, "w", encoding="utf-8") as f:
    json.dump(output_data, f, ensure_ascii=False, indent=2)

print(f"✅ 変換完了: {output_file}")
print(f"   → 処方数: {len(formulas_list)}")
```

---

## ✅ 実行結果

- ✅ 数秒で完了
- ✅ 294方剂すべてが配列形式に
- ✅ そのまま開発チームに渡せる状態

ご不明点があれば、実行前に出力されるJSONの構造をここで確認することも可能です。