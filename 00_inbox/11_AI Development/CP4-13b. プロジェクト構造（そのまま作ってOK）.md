HealthBook Agentで扱う、211方剤JSON、200問診JSONは既にあります。よって、MBT55 Agent の科学モデルを数式化し、VS Code で実装する、を進めましょう。

重複

---

いいですね、ここからは「実装モード」に振り切りましょう。  
まずは **MBT55 Agent の最小コア**を、VS Code にそのまま貼れる形で出します。

---

### 1. 数理モデルのコア（今回実装する範囲）

使う式はこの3つです。

1. **SOC ダイナミクス**

\[
\frac{dC_s}{dt} = I - k_d \cdot C_s + f_m(\text{MBT55}) \cdot \eta
\]

2. **電子フロー**

\[
J_{e^-} = \sum_i \left( k_i \cdot [S_i] \cdot \phi_i \right)
\]

3. **炭素固定効率**

\[
C_f = \lambda \cdot \frac{E_d}{E_d + K_m}
\]

ここまでを **v0 の MBT55 Agent** としてコード化します。

---

### 2. プロジェクト構造（そのまま作ってOK）

```text
mbt55-agent/
  README.md
  pyproject.toml or requirements.txt
  /src/mbt55_agent/
    __init__.py
    models.py
    electron_flow.py
    carbon_cycle.py
    hypercycle.py   # 後で
    simulate.py     # 後で
```

---

### 3. `models.py`（そのまま貼ってOK）

```python
from dataclasses import dataclass
from typing import Dict


@dataclass
class SoilState:
    """土壌状態の基本パラメータ"""
    soc: float          # Soil Organic Carbon [t-C/ha]
    cn_ratio: float     # C/N 比
    ph: float
    moisture: float     # [%]
    temperature: float  # [°C]


@dataclass
class OrganicInput:
    """投入有機物の組成"""
    carbon: float       # [t-C/ha/year]
    nitrogen: float     # [t-N/ha/year]
    lignin_ratio: float
    lipid_ratio: float
    protein_ratio: float
    starch_ratio: float


@dataclass
class MBT55Params:
    """MBT55 のモデルパラメータ"""
    kd: float                 # 分解定数 k_d [1/year]
    lambda_cf: float          # 最大炭素固定率 λ
    km_ed: float              # 電子飽和定数 K_m
    eta: float                # 電子再固定効率 η (0–1)
    guild_activity: Dict[str, float]  # 各ギルドの活動強度（0–1）
    # 例: {"starch": 0.8, "protein": 0.7, "lipid": 0.6, "cellulose": 0.9}
```

---

### 4. `electron_flow.py`

```python
from .models import SoilState, OrganicInput, MBT55Params


def compute_electron_flow(
    soil: SoilState,
    org: OrganicInput,
    params: MBT55Params,
) -> float:
    """
    電子フロー J_e を計算する簡易モデル。
    ギルド別の基質量 × 活動強度 × 経路効率を足し合わせる。
    """

    # 基質量の近似（全炭素に対する比率で重み付け）
    substrate = {
        "starch": org.carbon * org.starch_ratio,
        "protein": org.carbon * org.protein_ratio,
        "lipid": org.carbon * org.lipid_ratio,
        "cellulose": org.carbon * org.lignin_ratio,
    }

    # 仮の経路効率 φ_i（後でキャリブレーション）
    phi = {
        "starch": 1.0,
        "protein": 0.9,
        "lipid": 1.2,
        "cellulose": 0.7,
    }

    je = 0.0
    for guild, s in substrate.items():
        k_i = params.guild_activity.get(guild, 0.0)
        phi_i = phi.get(guild, 0.0)
        je += k_i * s * phi_i

    # 環境補正（pH・水分などを簡易に反映）
    env_factor = _environment_factor(soil)
    return je * env_factor


def _environment_factor(soil: SoilState) -> float:
    """
    pH・水分・温度から環境係数をざっくり計算。
    後で詳細モデルに差し替え可能なプレースホルダ。
    """
    ph_factor = 1.0 - abs(7.0 - soil.ph) * 0.1
    moisture_factor = 0.5 + min(soil.moisture, 100.0) / 200.0
    temp_factor = 0.5 + max(0.0, min(soil.temperature, 40.0)) / 80.0

    return max(0.0, ph_factor * moisture_factor * temp_factor)
```

---

### 5. `carbon_cycle.py`

```python
from .models import SoilState, OrganicInput, MBT55Params
from .electron_flow import compute_electron_flow


def compute_carbon_fixation_efficiency(
    electron_donor_eff: float,
    params: MBT55Params,
) -> float:
    """
    C_f = λ * E_d / (E_d + K_m)
    """
    ed = max(electron_donor_eff, 0.0)
    return params.lambda_cf * ed / (ed + params.km_ed) if ed > 0 else 0.0


def compute_soc_delta(
    soil: SoilState,
    org: OrganicInput,
    params: MBT55Params,
) -> float:
    """
    dC_s/dt = I - k_d * C_s + f_m(MBT55) * η
    ここでは f_m を C_f とみなし、電子フローから計算する。
    """
    je = compute_electron_flow(soil, org, params)
    cf = compute_carbon_fixation_efficiency(je, params)

    dcs_dt = org.carbon - params.kd * soil.soc + cf * params.eta
    return dcs_dt
```

---

これで、**「MBT55 Agent v0：炭素・電子フローの科学エンジン」**が立ち上がります。

次の一手としては：

- このコードを VS Code に貼る  
- 仮のパラメータで `compute_soc_delta` を叩く小さな `demo.py` or Notebook を作る  
- その結果を見ながら、**パラメータ（λ, K_m, η, guild_activity）を一緒にキャリブレーション**していく

ここまで貼ったら、「実行してみたログ」か「気になる挙動」を教えてくれたら、次は **ハイパーサイクル指標 H と GHG 削減モデル**を足していきましょう。